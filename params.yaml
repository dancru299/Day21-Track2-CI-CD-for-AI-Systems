model_type: random_forest # random_forest, gradient_boosting, logistic_regression

# Sieu tham so chung (tuy thuoc vao model)
n_estimators: 200
max_depth: 20
min_samples_split: 2
max_iter: 100 # Cho LogisticRegression
